"""Used only when classify_by_rule() returns None. Logged with confidence score."""

def classify_with_llm(event_context: dict) -> tuple[str, float]:
    """Stub — returns (root_cause, confidence)."""
    raise NotImplementedError
