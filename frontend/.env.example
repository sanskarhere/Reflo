# URL of the Reflo FastAPI backend (Render), no trailing slash.
# Local dev against a locally-running backend:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=https://reflo-backend.onrender.com
