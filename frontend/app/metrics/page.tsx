// Screen 3: Batch Metrics — recovered amount vs naive-retry baseline.
export default function MetricsPage() {
  return <main>Batch metrics — recovered amount, recovery rate, baseline comparison.</main>;
}
